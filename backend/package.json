{
  "name": "estateinvestmentbot_backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate:schema": "apollo schema:download --endpoint=http://localhost:3000/graphql",
    "generate:types": "npm run generate:schema && apollo codegen:generate --target=typescript --queries=../frontend/src/queries/*.tsx --schema schema.json --outputFlat ../types"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "api-types": "../types",
    "apollo": "^1.4.0",
    "serverless-offline": "^3.25.6",
    "serverless-plugin-typescript": "^1.1.5"
  },
  "dependencies": {
    "apollo-server": "^2.0.0-rc.10",
    "apollo-server-lambda": "^2.0.0-xrc.11",
    "graphql": "^0.13.2"
  }
}
